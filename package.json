{
  "name": "red-detector-web-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "test": "vitest",
    "compile": "tsc --noEmit",
    "bench": "vitest bench src/benchmarks/*.bench.ts",
    "postinstall": "wxt prepare"
  },
  "dependencies": {
    "@dagrejs/graphlib": "^4.0.1",
    "comctx": "latest",
    "solid-js": "latest"
  },
  "devDependencies": {
    "@fast-check/vitest": "latest",
    "@wxt-dev/module-solid": "latest",
    "typescript": "latest",
    "vitest": "latest",
    "wxt": "latest"
  },
  "aube": {
    "allowBuilds": {
      "esbuild": true,
      "spawn-sync": true
    }
  }
}
